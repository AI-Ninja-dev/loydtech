'use client';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { useEffect, useRef, useState } from 'react';
import { Menu, X, ArrowUpRight } from 'lucide-react';
const links=[['Solutions','/solutions'],['Sentinel 365','/products'],['Industries','/use-cases'],['About','/about'],['Resources','/resources']];
export default function Header(){const pathname=usePathname();const dialog=useRef<HTMLDialogElement>(null);const [open,setOpen]=useState(false);useEffect(()=>{if(!open)return;const prior=document.body.style.overflow;document.body.style.overflow='hidden';return()=>{document.body.style.overflow=prior}},[open]);function close(){dialog.current?.close();setOpen(false)}return <header className="header"><div className="wrap header-inner"><Link className="wordmark" href="/" aria-label="Loydtech home">LOYDTECH<span>DIGITAL SOLUTIONS</span></Link><nav className="desktop-nav" aria-label="Main navigation">{links.map(([label,href])=><Link key={href} href={href} aria-current={pathname===href?'page':undefined}>{label}</Link>)}</nav><Link className="header-cta" href="/contact">Let’s talk <ArrowUpRight size={16}/></Link><button className="menu-button" aria-label="Open navigation" aria-expanded={open} onClick={()=>{dialog.current?.showModal();setOpen(true)}}><Menu size={23}/></button><dialog ref={dialog} className="mobile-menu" onClose={()=>setOpen(false)} onClick={e=>{if(e.target===e.currentTarget)close()}}><div className="menu-top"><strong>LOYDTECH</strong><button aria-label="Close navigation" onClick={close}><X/></button></div><nav aria-label="Mobile navigation">{[...links,['Discuss your project','/contact']].map(([label,href])=><Link key={href} href={href} onClick={close}>{label}<ArrowUpRight size={20}/></Link>)}</nav><p>Connected systems. Clearer operations.</p></dialog></div></header>}
