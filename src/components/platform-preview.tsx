'use client';
import { useState } from 'react';
import { ArrowUpRight, MapPin, Radio, Activity, ChevronRight } from 'lucide-react';
import { Action } from './site-ui';
const modes=[{name:'Asset visibility',title:'A place for every asset.',copy:'Explore location, zones and movement history in one operational view.',metric:'Asset overview',value:'24',unit:'sample assets',icon:MapPin},{name:'Condition monitoring',title:'Context around every reading.',copy:'Bring measurements and equipment state together to support investigation.',metric:'Temperature trend',value:'4.2',unit:'°C · sample reading',icon:Radio},{name:'Operational workflows',title:'From signal to next step.',copy:'Explore how an event can become a reviewed, assigned operational task.',metric:'Review queue',value:'03',unit:'illustrative tasks',icon:Activity}];
export default function PlatformPreview(){const [active,setActive]=useState(0);const mode=modes[active];const Icon=mode.icon;return <div className="platform-explorer"><div className="platform-tabs" aria-label="Explore platform views">{modes.map((m,i)=><button key={m.name} aria-pressed={active===i} onClick={()=>setActive(i)}>{m.name}<ChevronRight size={16}/></button>)}</div><div className="platform-body"><div className="platform-description"><span className="eyebrow">SENTINEL 365</span><h3>{mode.title}</h3><p>{mode.copy}</p><Action href="/contact?topic=Sentinel%20365">Discuss the platform</Action><small>Preview illustrates the proposed experience. Scope and integrations are agreed per project.</small></div><div className="dashboard" aria-label={`${mode.name}: illustrative platform preview`}><div className="dashboard-top"><strong><span className="s365">S</span> Sentinel 365</strong><span>Illustrative preview</span></div><div className="dashboard-content"><div className="dashboard-title"><span>{mode.metric}</span><Icon size={22}/></div><strong className="dashboard-value">{mode.value}<small>{mode.unit}</small></strong>{active===0?<div className="site-map" aria-label="Illustrative site zones"><div className="zone z1">Warehouse</div><div className="zone z2">Operations</div><div className="zone z3">Loading area</div><span className="pin p1"/><span className="pin p2"/><span className="pin p3"/></div>:active===1?<svg className="demo-chart" viewBox="0 0 400 150" role="img" aria-label="Illustrative temperature trend, not live readings"><path d="M0 30H400M0 80H400M0 130H400" stroke="#d9e7df" strokeDasharray="4 5"/><path d="M0 100L35 105L70 80L100 85L130 55L160 65L190 35L220 65L250 75L280 55L310 65L340 40L370 50L400 30" fill="none" stroke="#3a8061" strokeWidth="3"/></svg>:<div className="demo-tasks">{['Review condition event','Confirm device availability','Assign operational follow-up'].map((t,i)=><div key={t}><span>0{i+1}</span>{t}<ArrowUpRight size={14}/></div>)}</div>}<div className="dashboard-bottom"><span>Example site</span><span>No live connection</span></div></div></div></div></div>}
