'use client';
import { useState, type FormEvent } from 'react';
import { useSearchParams } from 'next/navigation';
import { ArrowUpRight, Download } from 'lucide-react';
export default function Enquiry(){const params=useSearchParams();const topic=params.get('topic')||'';const [message,setMessage]=useState('');function prepare(form:HTMLFormElement){const data=new FormData(form);return `Loydtech project enquiry\n\nName: ${data.get('name')}\nEmail: ${data.get('email')}\nOrganisation: ${data.get('company')}\nProject area: ${data.get('topic')}\n\n${data.get('brief')}`;}function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const body=prepare(event.currentTarget);window.location.href=`mailto:info@loydtech.co.za?subject=${encodeURIComponent('Loydtech project enquiry')}&body=${encodeURIComponent(body)}`;setMessage('Your email app has been requested. Review and send the message there. This website has not submitted your enquiry.');}return <form key={topic} className="enquiry" onSubmit={submit}><div className="form-row"><label>Your name<input name="name" required autoComplete="name" maxLength={100}/></label><label>Work email<input name="email" type="email" required autoComplete="email" maxLength={150}/></label></div><label>Organisation<input name="company" autoComplete="organization" maxLength={150}/></label><label>Project area<input name="topic" defaultValue={topic.slice(0,150)} placeholder="For example, asset tracking or temperature monitoring" maxLength={150}/></label><label>What would you like to understand or improve?<textarea name="brief" rows={5} required maxLength={1500} placeholder="Tell us about the site, assets or operational challenge."/></label><label className="consent"><input type="checkbox" required/>I understand this opens my email app for me to review and send. I will not include passwords, sensitive records or patient information.</label><button className="action" type="submit">Prepare enquiry<span><ArrowUpRight size={17}/></span></button><button className="download-link" type="button" onClick={e=>{const form=e.currentTarget.form!;if(!form.reportValidity())return;const url=URL.createObjectURL(new Blob([prepare(form)],{type:'text/plain'}));const a=document.createElement('a');a.href=url;a.download='loydtech-project-enquiry.txt';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);setMessage('Enquiry text downloaded. Send it to info@loydtech.co.za when ready.');}}><Download size={16}/>Download a copy instead</button><p role="status">{message}</p></form>}
