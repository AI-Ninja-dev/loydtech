import Link from 'next/link';
import { ArrowUpRight, Radio, MapPin, ChartNoAxesCombined } from 'lucide-react';
import { Action, Closing, Symbol } from '@/components/site-ui';
import PlatformPreview from '@/components/platform-preview';
import { solutions, industries, process } from '@/lib/content';
export default function Home(){return <><section className="hero"><div className="hero-image"/><div className="wrap hero-content"><span className="eyebrow">CONNECTED OPERATIONS / SOUTH AFRICA</span><h1>Connected systems.<br/><span>Clearer operations.</span></h1><p>Bring assets, equipment and environments into view. Build the connection between what happens on the ground and what your team does next.</p><div className="hero-actions"><Action href="/contact">Discuss your project</Action><Action href="/solutions" secondary>Explore solutions</Action></div><div className="hero-signoff"><span/>A more connected<br/>way to operate.</div></div><span className="image-caption">Illustrative connected infrastructure</span></section><div className="capability-strip"><div className="wrap">{[[MapPin,'Asset tracking & RTLS'],[Radio,'Remote monitoring'],[ChartNoAxesCombined,'IoT analytics']].map(([Icon,label])=>{const I=Icon as typeof MapPin;return <span key={String(label)}><I size={24} strokeWidth={1.4}/>{String(label)}</span>})}</div></div><section className="section wrap"><div className="section-heading"><div><span className="eyebrow">THE CONNECTED OPERATION</span><h2>Physical operations.<br/><span className="muted">Digital perspective.</span></h2></div><p>Equipment, spaces and assets all tell a story. Loydtech brings IoT, location and monitoring into the same conversation—starting with the operational problem you need to solve.</p></div><div className="solution-grid">{solutions.slice(0,4).map((s,i)=><Link className="solution-tile" href={`/solutions#${s.slug}`} key={s.slug}><div className="tile-top"><Symbol kind={s.icon}/><span>0{i+1}</span></div><h3>{s.title}</h3><p>{s.short}</p><span className="tile-link">Explore solution<ArrowUpRight size={19}/></span></Link>)}</div><Link className="text-link" href="/solutions">View all solution areas <ArrowUpRight size={17}/></Link></section><section className="platform-section"><div className="wrap"><div className="section-heading"><div><span className="eyebrow">MEET SENTINEL 365</span><h2>The operational picture.<br/>Brought together.</h2></div><p>A platform direction for asset visibility, sensor context and operational workflows. Explore the experience, then define the right scope for your organisation.</p></div><PlatformPreview/></div></section><section className="section wrap industry-section"><div><span className="eyebrow">BUILT AROUND YOUR WORLD</span><h2>Different environments.<br/><span className="muted">Connected challenges.</span></h2><p>Start with the site, the people and the process. Then choose the technology.</p><Action href="/use-cases" secondary>Explore applications</Action></div><div className="industry-list">{industries.map(([name,copy,slug])=><Link href={`/use-cases#${slug}`} key={name}><h3>{name}</h3><p>{copy}</p><ArrowUpRight size={21}/></Link>)}</div></section><section className="process-section"><div className="wrap"><div className="section-heading"><div><span className="eyebrow">FROM IDEA TO OPERATION</span><h2>Start focused.<br/>Build with purpose.</h2></div><p>A useful connected system starts with clear requirements, a practical pilot and a team that knows what happens next.</p></div><div className="process-grid">{process.map(([title,copy],i)=><article key={title}><span>0{i+1}</span><h3>{title}</h3><p>{copy}</p></article>)}</div></div></section><Closing/></>}
