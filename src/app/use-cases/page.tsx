import { Intro, Closing, Action } from '@/components/site-ui';
import { industries } from '@/lib/content';
export const metadata={title:'Industries & applications'};
const contexts=['Start with equipment identity, location requirements and the environments that need observation. Keep operational monitoring distinct from clinical decisions.','Define the journey, storage conditions, sampling needs and who investigates an excursion. Validate communication coverage along the route.','Map buildings, equipment and existing systems. Select a focused operational problem before introducing new layers of technology.','Understand differences between stores, stock areas and distribution sites. Design escalation around the people who can actually respond.','Establish the metering boundary, measurement quality and the comparisons needed for useful reporting.'];
export default function UseCases(){return <><Intro label="INDUSTRIES & APPLICATIONS" title="Technology meets the real world.">Explore where connected visibility can support operations. These are application scenarios, not claims about completed customer projects.</Intro><section className="wrap use-case-list">{industries.map(([name,copy,slug],i)=><article id={slug} key={name}><div className={`industry-art art-${i}`} aria-hidden="true"><span>{String(i+1).padStart(2,'0')}</span><div/><div/><div/></div><div><span className="eyebrow">APPLICATION SCENARIO</span><h2>{name}</h2><p className="lead">{copy}</p><p>{contexts[i]}</p><Action href={`/contact?topic=${encodeURIComponent(name)}`} secondary>Explore your use case</Action></div></article>)}</section><Closing/></>}
